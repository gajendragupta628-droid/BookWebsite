<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login — Motivational Books</title>
  <link rel="stylesheet" href="/public/css/app.css" />
  <link rel="stylesheet" href="/public/css/admin.css" />
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }
    
    .login-container {
      width: 100%;
      max-width: 450px;
    }
    
    .login-header {
      text-align: center;
      margin-bottom: 2rem;
    }
    
    .login-logo {
      font-size: 3rem;
      margin-bottom: 1rem;
    }
    
    .login-title {
      font-size: 1.75rem;
      font-weight: 700;
      background: linear-gradient(135deg, #f59e0b, #fbbf24);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 0.5rem;
    }
    
    .login-subtitle {
      font-size: 0.875rem;
      color: var(--admin-text-muted);
    }
    
    .login-card {
      background: rgba(26, 26, 26, 0.8);
      backdrop-filter: blur(12px);
      border: 1px solid var(--admin-border);
      border-radius: 1rem;
      padding: 2.5rem;
      box-shadow: var(--admin-shadow-lg);
    }
    
    @media (max-width: 640px) {
      .login-card {
        padding: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <div class="login-logo">📚</div>
      <h1 class="login-title">Admin Portal</h1>
      <p class="login-subtitle">Motivational Books Management System</p>
    </div>
    
    <div class="login-card">
      <% if (error) { %>
        <div class="admin-alert admin-alert-danger" style="margin-bottom: 1.5rem;">
          <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span><%= error %></span>
        </div>
      <% } %>
      
      <form method="post" action="/admin/login" class="admin-form">
        <div class="admin-form-group">
          <label class="admin-form-label required">Email Address</label>
          <input 
            type="email" 
            name="email" 
            class="admin-form-input" 
            value="<%= (typeof prefillEmail === 'string') ? prefillEmail : '' %>"
            placeholder="<%= (typeof prefillEmail === 'string' && prefillEmail) ? prefillEmail : 'admin@example.com' %>" 
            required 
            autofocus
          />
          <% if (typeof prefillEmail === 'string' && prefillEmail) { %>
            <div class="admin-form-hint">Configured admin email: <%= prefillEmail %></div>
          <% } %>
        </div>
        
        <div class="admin-form-group">
          <label class="admin-form-label required">Password</label>
          <input 
            type="password" 
            name="password" 
            class="admin-form-input" 
            placeholder="Enter your password" 
            required
          />
        </div>
        
        <button type="submit" class="admin-btn admin-btn-primary admin-btn-lg" style="width: 100%; margin-top: 1.5rem;">
          <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
          </svg>
          Sign In to Dashboard
        </button>
      </form>
      
      <div style="margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--admin-border); text-align: center;">
        <p style="font-size: 0.875rem; color: var(--admin-text-muted);">
          Secure admin access only
        </p>
      </div>
    </div>
    
    <div style="text-align: center; margin-top: 2rem; font-size: 0.75rem; color: var(--admin-text-muted);">
      <p>&copy; <%= new Date().getFullYear() %> Motivational Books. All rights reserved.</p>
    </div>
  </div>
  
  <script>
    // Add subtle animation on load
    document.addEventListener('DOMContentLoaded', function() {
      const card = document.querySelector('.login-card');
      card.style.opacity = '0';
      card.style.transform = 'translateY(20px)';
      
      setTimeout(() => {
        card.style.transition = 'all 0.5s ease-out';
        card.style.opacity = '1';
        card.style.transform = 'translateY(0)';
      }, 100);
    });
  </script>
</body>
</html>
