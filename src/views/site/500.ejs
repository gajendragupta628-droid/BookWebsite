<div class="error-page">
  <div class="error-container">
    <div class="error-icon-wrapper">
      <svg class="error-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 9V2m0 7l4-4m-4 4l-4-4m4 11v7m0-7l4 4m-4-4l-4 4"/>
      </svg>
    </div>
    <h1 class="error-code">500</h1>
    <h2 class="error-title">Server Error</h2>
    <p class="error-description">
      <%= message || 'Something went wrong on our end. We\'re working to fix it.' %>
    </p>
    <div class="error-actions">
      <a href="/" class="btn-primary">Go to Homepage</a>
      <button onclick="window.location.reload()" class="btn-secondary">Try Again</button>
    </div>
  </div>
</div>

<style>
.error-page {
  background: #fafafa;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
}

.error-container {
  max-width: 500px;
  width: 100%;
  text-align: center;
}

.error-icon-wrapper {
  width: 120px;
  height: 120px;
  margin: 0 auto 32px;
  background: rgba(231, 76, 60, 0.08);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.error-icon {
  width: 60px;
  height: 60px;
  color: #e74c3c;
}

.error-code {
  font-family: 'Playfair Display', serif;
  font-size: clamp(72px, 15vw, 120px);
  font-weight: 700;
  color: #0d0d0f;
  margin-bottom: 16px;
  line-height: 1;
  letter-spacing: -0.02em;
}

.error-title {
  font-family: 'Playfair Display', serif;
  font-size: clamp(28px, 5vw, 36px);
  font-weight: 600;
  color: #0d0d0f;
  margin-bottom: 16px;
  letter-spacing: -0.01em;
}

.error-description {
  font-size: 16px;
  color: rgba(13, 13, 15, 0.6);
  line-height: 1.6;
  margin-bottom: 40px;
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
}

.error-actions {
  display: flex;
  gap: 12px;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-primary,
.btn-secondary {
  padding: 14px 32px;
  border-radius: 8px;
  font-weight: 500;
  font-size: 16px;
  text-decoration: none;
  transition: all 0.2s ease;
  min-height: 48px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: none;
  cursor: pointer;
  font-family: inherit;
}

.btn-primary {
  background: #0d0d0f;
  color: white;
  box-shadow: 0 2px 8px rgba(13, 13, 15, 0.15);
}

.btn-primary:hover {
  background: #1a1a1d;
  box-shadow: 0 4px 12px rgba(13, 13, 15, 0.25);
  transform: translateY(-1px);
}

.btn-secondary {
  background: white;
  color: #0d0d0f;
  border: 1px solid rgba(13, 13, 15, 0.12);
}

.btn-secondary:hover {
  border-color: rgba(13, 13, 15, 0.25);
  background: rgba(13, 13, 15, 0.02);
}

@media (max-width: 640px) {
  .error-actions {
    flex-direction: column;
  }
  
  .btn-primary,
  .btn-secondary {
    width: 100%;
  }
}
</style>
